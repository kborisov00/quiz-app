export default class Quiz {
  constructor(config) {
    this.questions = config.questions;
    this.questionNode = config.questionNode;
    this.quizAnswersNode = config.quizAnswersNode;
  }

  // startQuiz() {}

  // previousQuestion() {}

  // nextQuestion() {}
}
